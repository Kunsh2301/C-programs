/* Write a C program to calculate area of a rectangle:
a. Using hard coded inputs.*/
#include<stdio.h>

int main()
{
    int length,breadth;
    length=34,breadth=67;
    printf("the area of rectangle is %d",length*breadth);
    return 0;
}
